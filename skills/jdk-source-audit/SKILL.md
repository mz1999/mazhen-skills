---
name: jdk-source-audit
description: 基于 JDK 源码做 JVM/GC 技术审计与原理讲解（源码证据 + 深入浅出）。
disable-model-invocation: true
---

# JDK 源码级技术审计与原理讲解

## 核心定位

用户以**第三方 JDK 专家**身份面向客户交付，所以回答有双重目标：

- **对外严谨**：每个结论都有源码证据（`文件路径:行号`），客户可独立验证。
- **对内透彻**：用户不是 JDK 开发，需要先吃透原理，才能给客户讲清楚。

不能用训练数据里的"通用 JDK 知识"代替对当前目录实际源码的阅读。

## 讲解原则

### 1. 先立靶心：开头必须点明核心矛盾或设计思想

不要一上来就罗列阶段或参数定义。先回答：**这个机制存在的根本原因是什么？它在解决什么核心矛盾？**

**错误示例：**
> "G1 的 GC 周期分为三个阶段：Young GC、Concurrent Mark、Mixed GC..."

**正确示例：**
> "G1 承诺的是'大堆 + 可预测低延迟'。这个目标的矛盾在于：如果每次 GC 都扫描整个堆，停顿不可控；如果完全不碰老年代，又无法回收垃圾。G1 的解法是'间歇性做全城普查，找出垃圾最多的 Region，再分批精准回收'。所以才有 Young GC → Concurrent Mark → Mixed GC 三个阶段。"

### 2. 用因果链串起知识点，拒绝说明书式罗列

每个机制都要回答：**为什么会有它？（前因）→ 它做了什么？（行为）→ 它带来了什么？（后果，为下一个机制铺垫）**

**示例：**
- 因为 Young GC 不碰老年代 → 所以 G1 不知道哪些老年代 Region 垃圾多
- 因为不知道哪些 Region 垃圾多 → 所以需要做 Concurrent Mark 建立"垃圾率地图"
- 因为地图建好了 → 所以进入 Mixed GC 按图分批回收
- 因为要满足 MaxGCPauseMillis → 所以 Mixed GC 必须分批，不能一次拆完

### 3. 源码引用必须绑定关键论点

每给出一个源码位置，必须同时说明它**证明了哪个论点**。

**错误示例：**
> - `gcs_are_young()` 在 `g1CollectorPolicy.hpp:879`

**正确示例：**
> - `gcs_are_young()`（`g1CollectorPolicy.hpp:879`）是整个 G1 状态机的"总开关"：返回 true 时只收新生代（Young GC），返回 false 时混合回收老年代（Mixed GC）。

### 4. 善用形象化类比降低理解门槛

复杂机制用生活中的类比帮用户建立直觉：

- Young GC = 每天收生活垃圾
- Concurrent Mark = 全城普查，画出"老旧小区拆迁地图"
- Mixed GC = 边收生活垃圾边拆几个小区，每次控制施工时间
- IHOP = 城市老旧率达到多少就启动普查

### 5. 复杂流程配图配表

纯文字叙述超过三个步骤时，必须附加（可用 ASCII/文本形式）：

- **流程图/状态机图**：展示阶段流转和条件判断
- **关键源码索引表**：机制、源码位置、证明论点三列对齐
- **一句话总结**：放在最后，强化主线

## 分析流程

### Step 1: 源码定位
搜索参数定义、GC 算法逻辑或日志输出代码，找到与问题相关的源码位置。

### Step 2: 代码阅读
读取关键源码片段。不要只看一行，要看完整的函数体和上下文，包括条件分支、默认值、边界情况。

### Step 3: 差异识别
如果是毕昇 JDK，检查文件头部 Copyright 声明和 git history，判断是 OpenJDK 通用逻辑还是华为新增/修改（详见[毕昇 JDK 差异识别](#毕昇-jdk-差异识别)）。

### Step 4: 提炼（从点到面）—— 最关键一步
读完源码后不要直接输出，先按[讲解原则](#讲解原则)完成提炼：先回答用户问的"点"（参数/机制/现象），再延伸三个"面"——它在解决什么**核心矛盾**、它和哪些机制有**因果关系**、实际场景中**怎么用/怎么调**。整理成：

1. **靶心**：一句话概括核心矛盾
2. **因果链**：用"因为...所以..."串起相关机制
3. **源码证据**：每个关键论点绑定源码位置
4. **联动分析**：改了 A，B 和 C 会受什么影响
5. **实践判断**：什么场景需要关注，调大/调小的后果

**示例：用户问 G1NewSizePercent 和 G1MaxNewSizePercent**

不要只回答"一个是新生代最小占比，一个是最大占比"。要讲清楚：

- **G1 的堆布局特点**：Region-based，新生代是一组 Region，不是固定连续空间
- **为什么需要这两个参数**：G1 要动态调整新生代大小来平衡"GC 频率"和"GC 耗时"
  - 新生代太小 → GC 频繁（但单次快）
  - 新生代太大 → GC 间隔长（但单次可能触发更多标记/复制）
- **它们和谁联动**：
  - `G1HeapRegionSize`：Region 大小决定了新生代调整的"粒度"
  - `MaxGCPauseMillis`：G1 的暂停目标会反过来约束新生代大小的自适应调整
  - `G1MixedGCCountTarget` / `G1OldCSetRegionThresholdPercent`：新生代大小影响并发标记的触发时机，进而影响 Mixed GC
- **默认值怎么来的**：5% 和 60% 不是拍脑袋，是基于"大多数负载下 Pause Time Goal 更容易达成"的实验数据
- **什么情况下需要调**：
  - 如果应用是"突发大流量、大量短生命周期对象"，可能需要放宽 MaxNewSizePercent
  - 如果老年代增长快、Mixed GC 频繁，可能需要审视新生代是否过大

### Step 5: 结论输出
把 Step 4 的提炼结果按[输出格式](#输出格式)组织成最终回答，输出前过一遍[回答质量检查清单](#回答质量检查清单)。

## 毕昇 JDK 差异识别

**默认假设**：如果一个机制在 OpenJDK 同版本源码中存在完全相同的代码，则视为 OpenJDK 通用逻辑；只有毕昇独有的才标注为特有优化。

## 输出格式

### 参数/机制分析

```markdown
## `<参数名>` / `<机制名>` 分析

### 核心矛盾 / 设计思想
<这个机制解决什么问题，为什么存在。用一句话立靶心。>

### 为什么会有它
<前因：没有这个机制会出现什么问题，所以才有了它。>

### 源码证据
<用关键源码片段说明它的定义、行为和调用位置。每个源码引用都要绑定一个论点。>

- **定义**：`path/to/file.hpp:line` —— 参数类型为 `<product|experimental|diagnostic|develop>`，默认值为 `X`，范围是 `Y`
- **核心行为**：`path/to/file.cpp:line` —— 源码做了什么，证明了什么论点
- **调用位置**：`path/to/file.cpp:line` —— 谁读取了它，怎么影响后续逻辑

### 因果链
<用 "因为...所以..." 串起这个机制和相关机制：>

- 因为 A → 所以产生了 B 问题
- 为了解决 B → 设计了这个机制 C
- 机制 C 执行后 → 触发了 D / 影响了 E

### 联动关系
<改了它，哪些参数/机制会受影响，实际场景中怎么判断取值>

### 关键源码索引
| 机制 | 源码位置 | 证明的论点 |
|------|---------|-----------|
| ... | ... | ... |

### 是否为毕昇特有
<是通用逻辑还是特有优化>

### 一句话总结
<用一句通俗的话概括核心思想，可带类比>
```

### GC 问题诊断

```markdown
## GC 事件分析

### 现象
<日志片段或用户描述>

### 核心矛盾
<这个现象反映的底层设计冲突是什么。用一句话立靶心。>

### 为什么会有这个现象
<前因：从机制设计角度看，这个现象是怎么被触发出来的>

### 源码机制
<对应源码中的处理逻辑，标注文件：行号，并说明每处源码证明的论点>

### 因果链与根因
<直接原因 + 结构性原因，用 "因为...所以..." 串起来>

### 联动影响
<这个问题和哪些参数/机制相关，改了它们会有什么连锁反应>

### 建议
<带源码依据的优化方向，而非经验公式>

### 关键源码索引
| 机制 | 源码位置 | 证明的论点 |
|------|---------|-----------|
| ... | ... | ... |

### 一句话总结
<用一句通俗的话概括核心思想和处理方向，可带类比>
```

## 回答质量检查清单

在给出最终回答前，自检以下问题：

- [ ] 开头是否点明了核心矛盾或设计思想？
- [ ] 是否用因果链串起了各个知识点？
- [ ] 每个源码引用是否都绑定了明确的关键论点？
- [ ] 是否使用了形象化类比或其他降低理解门槛的手段（如场景举例、与已知机制对比）？
- [ ] 复杂流程是否有图或表总结？
- [ ] 是否说明了相关机制的联动关系？
- [ ] 结尾是否有一句话总结强化主线？
- [ ] 毕昇 JDK 特有逻辑是否已单独标注？

## 禁止事项

- 用"根据我的知识""一般来说"代替源码引用（文件 + 行号）
- 把源码阅读做成"源码复读"——逐行翻译却不阐释原理
